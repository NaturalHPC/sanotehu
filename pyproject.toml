[build-system]
requires = ["setuptools", "setuptools-scm[simple]"]
build-backend = "setuptools.build_meta"

[project]
name = "sanotehu"
description = "Deployment automation test package"
dynamic = ["version"]

